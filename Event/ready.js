module.exports = async (client) => {
    //You can do whatever you need here ;)
    client.user.setActivity('Bot Made By Lotas-Dan(K1ko)| k!help', { type: 'WATCHING' });
    console.log(`${client.user.username} has been logged in and is ready to use!`);
};